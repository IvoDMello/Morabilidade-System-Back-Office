fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.7.0
pydantic[email]
python-dotenv==1.0.1
python-multipart==0.0.20

# Supabase (banco + auth)
supabase==2.10.0

# E-mail transacional
resend==2.5.1

# HTTP client async
httpx>=0.26,<0.28

# Utilidades
pillow==11.0.0
python-slugify==8.0.4

# Geração de PDF (demonstrativos mensais de locação) — pure-Python, sem deps nativas.
reportlab==4.2.5

# Agendador interno (relatório automático de 30 dias) — roda no processo web.
APScheduler==3.10.4
# Base IANA de timezones: a imagem python:3.12-slim não inclui, e o ZoneInfo
# ("America/Sao_Paulo") do scheduler depende dela.
tzdata==2024.2

# Rate limiting
slowapi==0.1.9

# Monitoramento de erros
sentry-sdk[fastapi]==2.20.0
