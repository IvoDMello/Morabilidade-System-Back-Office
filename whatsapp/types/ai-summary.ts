/** Resumo de conversa gerado sob demanda pela IA — ver services/ai.service.ts. */
export interface ContactAiSummary {
  objetivo: string;
  orcamento: string;
  localizacao: string;
  estagio: string;
  proximosPassos: string;
}
